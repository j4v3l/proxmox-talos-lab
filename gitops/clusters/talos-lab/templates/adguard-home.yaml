apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: adguard-home
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "20"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ required "gitOpsRepoUrl is required" .Values.gitOpsRepoUrl | quote }}
    targetRevision: {{ .Values.gitOpsRevision | quote }}
    path: gitops/apps/adguard-home
    helm:
      releaseName: adguard-home
      values: |
        hosts:
          - {{ include "talos-lab.sslipHost" (dict "root" . "name" "adguard") }}
          - {{ include "talos-lab.labHost" (dict "root" . "name" "adguard") }}
        dnsLoadBalancerIP: {{ .Values.adguardDnsIp }}
  destination:
    server: https://kubernetes.default.svc
    namespace: adguard-home
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
